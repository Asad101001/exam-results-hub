// Minimal floating shapes component - subtle ambient decoration
export function FloatingShapes({ count = 5 }: { count?: number }) {
  // Simplified - just returns null for a cleaner production look
  // The ambient glow in the background provides sufficient visual interest
  return null;
}
